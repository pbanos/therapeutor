# <%= name %>

Write here something about the <%= name %> questionnaire application.

This project is generated with [therapeutor](https://github.com/pbanos/therapeutor): a parametrization of an application generated with [yo angular generator](https://github.com/yeoman/generator-angular) version 0.11.1.

## Prerequisites

* Make sure [Node.js](https://nodejs.org/en/download/package-manager/) is installed
* Install yeoman generator-angular dependencies

      npm install -g grunt-cli bower yo generator-karma generator-angular


## Build & development

Run `grunt` for building and `grunt serve` for preview.

## Testing

Running `grunt test` will run the unit tests with karma.
